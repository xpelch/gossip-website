# Agent-first setup

The connection page now leads with one copyable prompt for the user's agent.
The prompt includes the immutable signing specification, development endpoint
limitations, client compatibility checks, and a final agent_access verification.
It does not represent a working installer or certify an available local engine.

The full protocol guide remains inside a native Technical details disclosure.
Hash navigation opens that disclosure for existing section links. Without
JavaScript, the prompt can be selected manually and the disclosure still works.
The approved floating artwork is unchanged; chain image experiments were not used.

Validation: five Node checks pass. Browser checks pass at 360, 768 and 1440px,
including exact prompt clipboard content (normalizing Windows CRLF), denied
clipboard fallback, technical deep links, manual no-JavaScript disclosure and
existing interactions. Desktop and mobile setup screenshots inspected.
