# Validation v2 — Gossip / Signal

Date: 2026-09-06 America/Toronto (2026-09-07 UTC). This historical design report
superseded the v1 public-preview evidence at that time. Current deployment and kit
pins are recorded in `public/availability.json` and must take precedence.

## Design evidence

User selected Signal (graphite/orange/metal) before implementation. Refero craft
references and visual captures of Raycast, Privy and Kaito informed a documented
reference lock. Live Refero research tools were not available; no Refero results
are fabricated. The original generated speech sculpture was inspected and placed
in the website. Desktop and 360px mobile hero and guide inspected in Chromium.

Contrast calculated with WCAG sRGB luminance: body/canvas 16.59:1; secondary
text/privacy surface 7.14:1; primary CTA 7.04:1; art caption/canvas 5.83:1;
orange code label/canvas 7.21:1. Hover motion removed under reduced-motion.

## Product contract evidence

Coordinator independently inspected upstream signing reference, GossipSubmission
and AgentContributionTools from revision `47695b9bbc86658b1c420810fcb304c5ea68fb79`.
A Luna worker documented the contract; coordinator corrected the public-key byte
wording and incorporated the user's localhost choice. Gossip is the public brand;
Sherwood protocol strings and header names remain unchanged.

The current availability record publishes the reachable endpoint
`https://api.gossip-protocol.xyz/mcp`, capabilities endpoint
`https://api.gossip-protocol.xyz/v2/gossip/capabilities`, and exact audience
`https://api.gossip-protocol.xyz/`. Capabilities, MCP initialize and tools-list
diagnostics returned 200; replay and invalid-signature diagnostics returned 401. These outcomes do not verify production or a host. The record keeps
`productionVerified` and `hostAcceptanceVerified` false, with HTTP installed,
atomic consultation/durable operations/signed receipts/evidence verified against
the deployed engine evidence revision, public and private submissions blocked,
and session/tasks not applicable. The submission persistence check found 0
operations and 0 receipts.

## Local checks

Run `npm run check` and the browser CLI function in the README. Final outcomes are
recorded below after execution. Screenshots remain local under ignored
`output/playwright/`. The generated brand image is a tracked public asset.

## Limits

This website verifies its own rendering, controls and local HTTP boundary. It does
not validate production deployment, a third-party signing adapter/client version,
indexing coverage, live allowances, expiry/replay rejection or server revocation.
Source-backed implementation descriptions are not a claim of a live public offer.
No independent user comprehension study or full assistive-technology certification
is claimed. No shared root build/deployment, backend, indexer, DB or Railway changes.

### Final results

- Four Node checks passed: development/public metadata and guide consistency,
  internal links/no intake, actual local HTTP allowlist/security headers/write
  denial, and no script network/storage behavior.
- Coordinator ran the browser checks successfully on final public files at
  360/768/1440px for both pages: no horizontal overflow, valid anchors and one h1.
- Hero-to-guide navigation, query click/keyboard selection and actual answer text,
  route explanation, native FAQ, real clipboard read-back and denied-clipboard
  fallback, skip link, reduced motion and no-JavaScript content passed.
- Captured requests remained on the local preview origin; no backend, wallet,
  analytics or third-party resource was contacted by the website checks.
- Full desktop landing and tablet guide captures inspected against the selected
  reference lock; mobile hero and guide separately inspected. No unresolved
  blocking visual issue identified in these viewports.
- A missing await in the drafted browser check was repaired by the coordinator
  before the successful final run. It affected test code, not website behavior.
- `git diff --check` passed. Node checks were authored by Luna and inspected by
  the coordinator; browser validation and final acceptance were coordinator-owned.
